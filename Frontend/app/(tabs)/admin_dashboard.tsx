import React from 'react';
import AdminDashboard from '../../components/admin/AdminDashboardTabs'; // Import the correct component

export default function AdminDashboardScreen() {
  // Render the imported AdminDashboard component, not itself
  return <AdminDashboard />;
}